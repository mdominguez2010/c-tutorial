#include <stdio.h>
#include <stdlib.h> 
#include <math.h> //must include to call math functions

int main() {

    const int NUM = 5;
    printf("%d\n", NUM);
    //num = 8;
    printf("%d\n", NUM);

    printf("Hello"); // constants as well

    return 0;
}