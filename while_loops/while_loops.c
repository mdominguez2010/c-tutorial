#include <stdio.h>
#include <stdlib.h> 
#include <string.h>

int main(){

    int index = 0;
    do {
        printf("I farted %d times\n", index);
        index ++;
    } while(index <= 9);

    return 0;
}