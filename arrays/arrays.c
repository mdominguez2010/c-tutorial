#include <stdio.h>
#include <stdlib.h> 
#include <math.h> //must include to call math functions


int main() {

    // int luckyNumbers[] = {4, 8, 15, 64, 99, 9, 110};
    // luckyNumbers[1] = 200; //assign int to index

    // int luckyNumbers[10];
    // luckyNumbers[1] = 80;
    // luckyNumbers[0] = 90;

    // printf("%d\n", luckyNumbers[0]);

    char phrase[20];

    return 0;
}